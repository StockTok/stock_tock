import React, { Component } from "react";
import { Text, StyleSheet, View } from "react-native";

export default class SignupScreen extends Component {
  render() {
    return (
      <View>
        <Text> Register display </Text>
      </View>
    );
  }
}

const styles = StyleSheet.create({});
